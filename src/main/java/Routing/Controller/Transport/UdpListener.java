package Routing.Controller.Transport;

import Routing.Configuration.RouteProvider;
import Routing.Model.ListenerSettings;
import io.vertx.core.AbstractVerticle;

/**
 * @author Robin Duda
 */
public class UdpListener extends AbstractVerticle {
    public UdpListener(RouteProvider provider, ListenerSettings listener) {

    }
}
