---
#this file should be loaded by the RealmContext in the chain RealmContext/InstanceContext/GameContext?
name: "Angel Oak"
limit: 50
instances: ['a', 'b', 'c']
interval: 50 # once per second.
# todo: instances should be scripted.
onTick:
  # respawn npcs
  # spawn nodes
  # spawn traps
  # open/close portals
onPlayerEnter:
# we can add an event afflictions here.
onPlayerLeave:
# remove an event affliction etc.