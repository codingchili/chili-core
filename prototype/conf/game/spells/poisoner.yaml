---
name: "poisoner"
description: "Applies poison to the target."
cooldown: 60.0
casttime: 2.5
target: caster
onCastBegin:
  jexl:
    //spells.energy(caster, -40);
    //log.accept('onCastBegin!');

    return true;
onCastComplete:
  jexl: |
    log.accept('casting completed');
    spells.afflict(target.creature, 'poison');